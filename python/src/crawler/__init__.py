"""site-crawler: a fast, single-domain web crawler.

For each page it visits, the crawler prints the page URL and every link found on
that page, following only links that stay within the starting domain.
"""

__version__ = "0.1.0"
