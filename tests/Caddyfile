:80 {
    root * /usr/share/caddy
    file_server

	@broken {
        path /broken_page.html
    }
    handle @broken {
        respond 500 /500.html
    }

	handle_errors {
		rewrite * /{err.status_code}.html
		file_server
	}
}