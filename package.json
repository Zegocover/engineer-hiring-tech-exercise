{
    "name": "web-crawler",
    "version": "1.0.0",
    "description": "A concurrent web crawler that stays within a single subdomain",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc -p tsconfig.build.json",
        "start": "npm run build && node dist/index.js",
        "dev": "ts-node src/index.ts",
        "test": "jest",
        "test:coverage": "jest --coverage"
    },
    "dependencies": {
        "axios": "^1.7.0",
        "cheerio": "^1.0.0"
    },
    "devDependencies": {
        "@types/jest": "^29.5.14",
        "@types/node": "^20.0.0",
        "jest": "^29.7.0",
        "ts-jest": "^29.1.0",
        "ts-node": "^10.9.0",
        "typescript": "^5.4.0"
    }
}
